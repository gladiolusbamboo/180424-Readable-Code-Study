// わからん

// 'v'の「要素 < pivot」が「要素 >= pivot」の前に来るように配置し直す
// それから「v[i] < pivot」になる最大の'i'を返す（なければ-1を返す）。
function partition(v, pivot){
  // 処理
}

/*******************
 改善例
********************/
// 実例を示す

// 'v'の「要素 < pivot」が「要素 >= pivot」の前に来るように配置し直す
// それから「v[i] < pivot」になる最大の'i'を返す（なければ-1を返す）。
// 実例：Partition([8,5,9,8,2], 8)の結果は[5,2|8,9,8]となり、1を返す
function partition(v, pivot){
  // 処理
}
